# Build artifacts
chimera_operator
target/
dist/
node_modules/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Database files
*.db
*.db-shm
*.db-wal

# Docker volumes and data
data/

# Environment files (local overrides)
.env
.env.*.backup
.env.backup
!.env.example
docker/env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp
*.md
# Track important project docs
!README.md
!AGENTS.md
!CLAUDE.md
!CHANGELOG.md
!docs/**/*.md
!ops/**/*.md
!docker/**/*.md
!scout/**/*.md
!tools/**/*.md
!ssl/README.md

# Secrets / generated TLS material
ssl/certbot/
ssl/work/
*.key
*.pem
*.crt
# Encrypted vault files — never commit even when empty.
# `config/secrets.enc` is the canonical vault path; `config/config.enc` was a
# misnamed relic that has been removed. The broad `*.enc` rule catches any
# future variant. `*.enc.bak` covers the backup created by `import_keypair`
# during rotation (in case cleanup fails or the process is killed mid-write).
config/secrets.enc
config/*.enc
config/*.enc.bak
# MaxMind GeoIP databases (licensed, downloaded at runtime)
docker/haproxy/geoip/*.mmdb

/.hypothesis
/operator/.hypothesis
/scout/.hypothesis
/.claude
.crush.json
/.kilo

# Test artifacts
playwright-report/
test-results/
*.webm

# Backup files
backups/
*.backup
evaluation/
# Evaluation runtime data directory
