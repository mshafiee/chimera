# Minimal rsyslog configuration for HAProxy
$ModLoad imuxsock

# Route HAProxy local0.* logs to /var/log/haproxy/haproxy.log
local0.* /var/log/haproxy/haproxy.log

# Stop processing after this rule
& stop