[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Ignore common warnings for existing codebase
ignore = ["E501", "W293", "I001"]

[tool.ruff.lint.per-file-ignores]
# Test files can have longer lines
"tests/**/*.py" = ["E501"]
