# Keep the docker build context (repo root) lean.
# NOTE: scout/, scripts/, docker/, ops/ build from the root context too —
# they must NOT be excluded here.
.git
target
data
logs
node_modules
web
docs
.kilo
*.md
.env
backups
